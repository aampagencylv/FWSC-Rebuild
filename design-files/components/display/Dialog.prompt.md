Modal on navy scrim.

```jsx
<Dialog open={open} onClose={close} eyebrow="Membership" title="Join the Coalition"
  actions={<><Button variant="ghost" onClick={close}>Cancel</Button><Button>Continue</Button></>}>
  Membership is open to registered Florida liveries and water-sport operators.
</Dialog>
```
