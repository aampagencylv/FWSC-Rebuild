Text input with tracked-caps label, hint, and error.

```jsx
<Input label="Vessel registration no." placeholder="FL-0000-XX" hint="As shown on your certificate" />
<Input label="Email" error="Enter a valid email address" required />
```
